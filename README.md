Boot0 - the basic boot level of the system in which stabilization operations are set, setting the processor state and transition to boot 1
Boot1 - displays a table, detects the system and performs the boot of the system kernel
Bootabi - stores and executes boot standards for boot 1